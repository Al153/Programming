int fib(int x)
	if ((x==0) or (x==1)) then
		return 1;
	else
		return fib(x-1) + fib(x-2);
	endif;
end;

void main()
	int i;
	for (i=0,i<10,i=i+1)
		print(fib(i));
	loop;
end;

int i;
int j;
print(i+j-5);
return 0;
import z;