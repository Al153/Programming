@int create_namespace()
	//////////////////////////////
	// Creates namespace object //
	//////////////////////////////
end;

@int Search_id(@int id, @int namespace)
	/////////////////////////////////////////////////
	// Check if the id is a name or numerical id   //
	/////////////////////////////////////////////////

	if (name id) then
		/////////////////////////////////
		// Search strings in namespace //
		/////////////////////////////////

		///////////////////////////////////////////////
		// If that fails search the global namespace //
		///////////////////////////////////////////////

	else
		/////////////////////////////
		// Search ids in namespace //
		/////////////////////////////

		/////////////////////////////////////////////////////
		// If that fails then seacrch the global namespace //
		/////////////////////////////////////////////////////
	endif;
end;

void save_to_namespace(@int id, @int value, @int namespace)
	if (name id) then 
		////////////////
		// add string //
		////////////////
	else
		//////////////////////
		// add numerical id //
		//////////////////////

	endif;

end;